<div class="col-span-12">

  <!-- PageHeading & Toolbar -->
  <div class="flex flex-col gap-6">
    <div class="flex flex-wrap justify-between items-center gap-4">
      <div class="flex flex-col gap-1">
        <h1 class="text-white text-3xl font-bold leading-tight tracking-tight">Tipos de Incidentes</h1>
        <p class="text-gray-400 text-base font-normal leading-normal">
          Cree, edite y elimine los tipos de incidentes para el seguimiento.
        </p>
      </div>

      <button
        class="flex h-11 items-center justify-center gap-2 overflow-hidden rounded-lg bg-primary px-5 text-sm font-bold leading-normal tracking-wide text-black transition-colors hover:bg-primary/90">
        <span class="material-symbols-outlined text-base">add_circle</span>
        <span class="truncate">Agregar Nuevo Tipo</span>
      </button>
    </div>
  </div>

  <!-- Search & Filters -->
  <div class="flex flex-col sm:flex-row gap-4 items-center mt-6">
    <div class="w-full sm:flex-1">
      <label class="flex flex-col min-w-40 h-12 w-full">
        <div class="flex w-full flex-1 items-stretch rounded-lg h-full">
          <div class="text-gray-400 flex border-none bg-[#2D3748] items-center justify-center pl-4 rounded-l-lg border-r-0">
            <span class="material-symbols-outlined">search</span>
          </div>
          <input
            id="buscar-incidentes"
            class="form-input flex w-full min-w-0 flex-1 resize-none overflow-hidden rounded-r-lg text-white focus:outline-0 focus:ring-2 focus:ring-primary/50 border-none bg-[#2D3748] h-full placeholder:text-gray-400 px-4 pl-2 text-sm"
            placeholder="Buscar por nombre o descripción..."
            type="text"
          />
        </div>
      </label>
    </div>
  </div>

  <!-- Tabla -->
  <div class="overflow-x-auto mt-6">
    <div class="overflow-hidden rounded-lg border border-white/10 bg-white/5">
      <table class="w-full table-auto">
        <thead class="border-b border-b-white/10 bg-white/5">
          <tr>
            <th class="w-1/3 px-6 py-4 text-left text-xs font-medium uppercase tracking-wider text-white/80">
              Nombre del Tipo
            </th>
            <th class="w-1/2 px-6 py-4 text-left text-xs font-medium uppercase tracking-wider text-white/80">
              Descripción
            </th>
            <th class="px-6 py-4 text-right text-xs font-medium uppercase tracking-wider text-white/80">
              Acciones
            </th>
          </tr>
        </thead>

        <tbody id="tabla-incidentes" class="divide-y divide-white/10">
          <% if (incidentTypes && incidentTypes.length > 0) { %>
            <% incidentTypes.forEach(incident => { %>
              <tr>
                <td class="px-6 py-4 text-white"><%= incident.name %></td>
                <td class="px-6 py-4 text-gray-400"><%= incident.description %></td>
                <td class="px-6 py-4 text-right">
                  <div class="flex justify-end gap-4">
                    <button class="text-gray-400 hover:text-primary transition-colors">
                      <span class="material-symbols-outlined">edit</span>
                    </button>
                    <button class="text-gray-400 hover:text-red-500 transition-colors">
                      <span class="material-symbols-outlined">delete</span>
                    </button>
                  </div>
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td class="px-6 py-4 text-white" colspan="3">No hay tipos de incidentes para mostrar.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

</div>
